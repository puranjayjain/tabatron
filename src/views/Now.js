import React, {PropTypes, Component} from 'react'

import moment from 'moment'

// Custom components

// icons

export default class Now extends Component {
  render() {
    // const style = {
    // }

    return (
      <div>
        Now has all the current tabs that are open or are being worked on
      </div>
    )
  }
}
